# meme-generator
Simple meme generator

- Three inputs at a minimum should display:
    top text
    bottom text
    image URL
- when the form is submitted, an image should be appended to the page with the top and bottom tet over the image

- a user should be able to remove a single image by clicking on it, or there should be some interface that shows a user how to delete an individual image